var angular = require('angular');
    // ComponentNameComponent = require('./component-name/component-name.component');

const Components = angular.module('app.components', []);

// Components
//     .component('componentName', ComponentNameComponent);

module.exports = Components.name;